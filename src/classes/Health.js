export default class Health {
    /**
     * @param {string} message 
     * @param {string} path 
     */
    constructor(
        message,
        path
    ) {
        this.message = message;
        this.path = path;
    }
}